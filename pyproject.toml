# ... existing code ...

[project]
# ... existing code ...
requires-python = ">=3.11"

dependencies = [
  # ... keep existing deps ...
  "typer>=0.12.0",
  "rich>=13.7.0",
  "pydantic>=2.6.0",
  "PyYAML>=6.0.1",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0.0",
  "pytest-cov>=5.0.0",
]

[project.scripts]
researchboss = "researchboss.cli:app"

# ... existing code ...